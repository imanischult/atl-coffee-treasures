// empty function will soon hold simple search/submit button 
import React from 'react';

const SearchButton = () => {
    return (
        <div>
            
        </div>
    );
};

export default SearchButton;